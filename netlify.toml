[build]
  command = "npm run build"
  publish = "dist/whc_website/browser"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback — serves index.html for all routes Angular handles
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
